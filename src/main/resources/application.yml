# application.yml
server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://10.95.16.85:3306/river_chart_luo_writ?characterEncoding=UTF8&autoReconnect=true&allowMultiQueries=true&rewriteBatchedStatements=true&serverTimezone=GMT%2B7&useSSL=false
    username: dev_rw
    password: z8E3SuIy.1MlCril8
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB


logging:
  level:
    com.example: DEBUG

# SpringDoc OpenAPI 配置 (适用于 SpringDoc 2.x + Spring Boot 3)
springdoc:
  api-docs:
    enabled: true                    # 启用API文档
    path: /v3/api-docs               # API文档JSON访问路径（默认值）
  swagger-ui:
    enabled: true                    # 启用Swagger UI
    path: /swagger-ui.html           # Swagger UI访问路径（默认值）
    tags-sorter: alpha               # 标签排序：alpha-字母排序，默认按Controller定义顺序
    operations-sorter: alpha         # 接口排序：alpha-字母排序，method-按HTTP方法排序
  show-actuator: false               # 不显示Actuator端点
  default-flat-param-object: true    # 扁平化参数对象

# OpenAPI JSON文件生成配置
openapi:
  json:
    auto-generate: true              # 是否在应用启动时自动生成JSON文件
    output-dir: ${user.dir}/docs     # 输出目录（默认为项目根目录的docs文件夹）
    file-name: river_chart_luo_writ.json  # 输出文件名

# 文件上传配置
file:
  upload:
    path: /tmp/uploads/
    url-prefix: /uploads/

myapp:
  feature:
    enabled: true
    max-items: 100